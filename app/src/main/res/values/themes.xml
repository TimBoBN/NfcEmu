<resources>
    <!-- android:windowBackground is set explicitly to match NfcEmuTheme's fixed dark
         Compose background exactly. Without this, the raw Android window background
         can flash through for a frame during activity start or screen transitions. -->
    <style name="Theme.NfcEmu" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- Transient theme shown only while the splash screen / first Compose frame render. -->
    <style name="Theme.NfcEmu.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.NfcEmu</item>
    </style>
</resources>
