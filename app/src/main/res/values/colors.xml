<resources>
    <!-- Matches Theme.kt's Nocturne background token so the raw Android window
         background can never visibly differ from what Compose draws (avoids a
         flash during activity start or screen transitions). Dark-only design -
         no values-night variant needed since there's no light theme at all. -->
    <color name="splash_background">#161826</color>
    <color name="window_background">#161826</color>
</resources>
